import { ReactNode } from 'react';
interface MenuLinkProps {
    children: ReactNode;
    to: string;
}
export default function MenuLink({ children, to }: MenuLinkProps): import("react/jsx-runtime").JSX.Element;
export {};
